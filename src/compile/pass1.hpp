#pragma once
#include <cish/compile-pass.hpp>

namespace cish
{
    struct CompilePass_1: public NodeVisitor
    {
        // virtual void visit_List      ( CompileCtx&, List& ) final;
        // virtual void visit_Atom      ( CompileCtx&, Atom& ) final;
        // virtual void visit_Program   ( CompileCtx&, Program& ) final;
        // virtual void visit_Stmnt     ( CompileCtx&, Stmnt& ) final;
        // virtual void visit_StmntList ( CompileCtx&, StmntList& ) final;
        // virtual void visit_Decl      ( CompileCtx&, Decl& ) final;
        // virtual void visit_Assign    ( CompileCtx&, Assign& ) final;
        // virtual void visit_Expr      ( CompileCtx&, Expr& ) final;
        // virtual void visit_VarDecl   ( CompileCtx&, VarDecl& ) final;
        // virtual void visit_FunDecl   ( CompileCtx&, FunDecl& ) final;
        // virtual void visit_DeclList  ( CompileCtx&, DeclList& ) final;
        // virtual void visit_Call      ( CompileCtx&, Call& ) final;
        // virtual void visit_CallList  ( CompileCtx&, CallList& ) final;
        // virtual void visit_Term      ( CompileCtx&, Term& ) final;
    };
}

